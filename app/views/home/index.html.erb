<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">BINGO</h1>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
    <%= form_for @game, url: games_path(@game), :html => { :class => 'form-inline' } do |f| %>
      <%= text_field_tag 'player[name]', '', class: 'form-control mb-2 mr-sm-2', placeholder: t(:player_name) %>
      <%= f.text_field :name, class: 'form-control mb-2 mr-sm-2', placeholder: t(:game_name) %>
      <%= f.hidden_field :slug %>
      <%= f.submit t(:play), class: 'btn btn-primary mb-2' %>
    <% end %>
  </div>
</section>
