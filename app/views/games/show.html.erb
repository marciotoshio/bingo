<h1><%= @game.name %> <%= link_to t(:reset), reset_games_path(slug: @game.slug), :class => "btn btn-danger" %></h1>

<div class="float-left">
  <%= render 'board' %>
</div>

<div class="float-left">
  <%= render 'game_controls' %>
</div>

<div class="float-left">
  <%= render 'player' %>
</div>
